[package]
name = "pallet-base"
version = "0.1.0"
authors = ["Polymath"]
edition = "2018"

[dependencies]
polymesh-primitives = { path = "../../primitives", default-features = false }
polymesh-common-utilities = { path = "../common", default-features = false }

# Others
serde = { version = "1.0.104", default-features = false }
serde_derive = { version = "1.0.104", optional = true, default-features = false  }

# Substrate
codec = { package = "parity-scale-codec", version = "2.0.0", default-features = false, features = ["derive"] }
scale-info = { version = "1.0", default-features = false, features = ["derive"] }
sp-core = { git = "https://github.com/PolymathNetwork/substrate", default-features = false, branch = "polymesh-develop" }
sp-std = { git = "https://github.com/PolymathNetwork/substrate", default-features = false, branch = "polymesh-develop" }
sp-io = { git = "https://github.com/PolymathNetwork/substrate", default-features = false, branch = "polymesh-develop" }
sp-runtime = { git = "https://github.com/PolymathNetwork/substrate", default-features = false, branch = "polymesh-develop" }
sp-version = { git = "https://github.com/PolymathNetwork/substrate", default-features = false, branch = "polymesh-develop" }
sp-api = { git = "https://github.com/PolymathNetwork/substrate", default-features = false, branch = "polymesh-develop" }
frame-system = { git = "https://github.com/PolymathNetwork/substrate", default-features = false, branch = "polymesh-develop" }
frame-support = { git = "https://github.com/PolymathNetwork/substrate", default-features = false, branch = "polymesh-develop" }

[features]
