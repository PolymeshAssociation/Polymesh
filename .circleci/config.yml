version: 2
jobs:
  lint:
    docker:
      - image: maxsam4/rust
    environment:
      VERBOSE: "1"
    steps:
      - checkout
      - run:
          name: Check formatting
          command: ./scripts/rustfmt.sh
          working_directory: .
  build:
    docker:
      - image: maxsam4/rust
    environment:
      VERBOSE: "1"
    steps:
      - checkout
      - restore_cache:
          keys:
            - build-cache-{{ checksum "./polymesh_substrate/Cargo.lock" }}
      - run:
          name: Build rocksdb
          command: cargo +nightly build -p rocksdb -j 1 --release
          working_directory: ./polymesh_substrate
      - run:
          name: Build release
          command: cargo +nightly build -j 1 --release
          working_directory: ./polymesh_substrate
      - run:
          name: Run unit tests
          command: cargo +nightly test
          working_directory: ./polymesh_substrate/runtime
      - save_cache:
          key: build-cache-{{ checksum "./polymesh_substrate/Cargo.lock" }}
          paths:
            - "~/.cargo"
            - "./polymesh_substrate/target"
            - "./polymesh_substrate/runtime/target"
            - "./polymesh_substrate/runtime/wasm/target"
workflows:
  version: 2
  commit:
    jobs:
      - lint
      - build
  daily-builds:
    triggers:
      - schedule:
          cron: "0 0 * * *"
          filters:
            branches:
              only:
                - master
    jobs:
      - build
