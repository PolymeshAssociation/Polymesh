[package]
name = "integration"
version = "0.1.0"
edition = "2021"

[features]
default = []

[dependencies]
anyhow = "1"
log = "0.4"

tokio = { version = "1.19", features = ["full"] }

sp-core = "21.0"
sp-runtime = "24.0"
sp-keyring = "24.0"

polymesh-api = { version = "3.2.1", features = ["download_metadata"] }
polymesh-api-tester = { version = "0.2.1" }
